create table users(
	ID integer primary key autoincrement,
	uname text not null,
	password text not null,
	curURL text);

create table challenges(
	ID integer primary key autoincrement,
	cname text not null,
	progress text);
